# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Guide
- Don't validate things in browser (project is only for iOS and Android)
- Use bun run check for validation (nothing else needed)
- Don't use Stylesheet, use inline styles instead